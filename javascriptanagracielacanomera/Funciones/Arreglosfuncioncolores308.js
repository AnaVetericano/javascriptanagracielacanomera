// Crear un arreglo de colores y agregar uno más al principio
// Instrucciones:
//Crea un arreglo con los colores "rojo", "verde", "azul".
// Agrega el color "amarillo" al principio del arreglo.
function colores()
{
let colores = [] // crea el dato
colores.push("rojo") // agrega el dato
console.log(colores) // muestra el dato
colores.push("verde") // agrega el dato
console.log(colores) // muestra el dato
colores.push("azul") // agrega el dato
confirm.log(colores) // muestra el dato
colores.unshift("amarillo") // agrega el dato
console.log(colores) // muestra el dato
}
colores()